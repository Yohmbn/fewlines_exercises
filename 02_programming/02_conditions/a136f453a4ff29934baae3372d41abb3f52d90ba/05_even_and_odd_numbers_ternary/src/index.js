let numberToCheck = 18;

// transform (or better, re do) the code of your previous exercise using a ternary operator.
let msg = numberToCheck % 2 === 0 ? "The number is even" : "The number is odd";
console.log(msg);